Finds minimum of the equation E(w) = (w + 1)(w − 1)(w − 3)(w − 4)
using gradient descent

If the program gets stuck at a local min/max, w is reinitalized until the known
minimum is achieved

Each adjustment to w is logged using pyplot and labeled with which iteration it was found